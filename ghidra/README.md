(mostly unsuccessful) attempt to run ghidra in headless mode to bruteforce the memory load-address of the .bin file.

"CountReferencedStrings.java" doesn't seem to work very well in a headless run like this.